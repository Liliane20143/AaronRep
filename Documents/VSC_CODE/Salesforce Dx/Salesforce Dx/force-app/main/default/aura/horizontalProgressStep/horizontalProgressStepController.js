({
/*	doInit: function(cmp, evt, hlp) {
		cmp.set('v.tooltipId', Math.floor((Math.random() * 100) + 1).toString());
	},

	showTooltip: function(cmp, evt) {
		cmp.set('v.showTooltip', true);
	},

	hideTooltip: function(cmp, evt) {
		cmp.set('v.showTooltip', false);
	},

	stateChanged : function(cmp, evt, hlp) {
		if (cmp.get('v.state') === 'active') {
			cmp.set('v.showTooltip', true);
			cmp.set('v.timeoutId', window.setTimeout($A.getCallback(() => {
				cmp.set('v.showTooltip', false);
			}), cmp.get('v.tooltipTimeout')));
		} else {
			cmp.set('v.showTooltip', false);
			clearTimeout(cmp.get('v.timeoutId'));
		}
	}*/
})